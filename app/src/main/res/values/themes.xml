<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Splash Screen Theme -->
    <style name="Theme.SplashScreen.FixIt" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="postSplashScreenTheme">@style/Theme.FixIt</item>
    </style>

    <!-- Main App Theme - This is what runs after splash -->
    <style name="Theme.FixIt" parent="android:Theme.Material.Light.NoActionBar" />
</resources>